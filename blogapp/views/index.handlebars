<div class="jumbotron">
  <h1 class="display-4">Bem vindo ao blog do Node!</h1>
  <p class="lead">Blog simples desenvolvido utilizando as tecnologias Node.JS + Express + MongoDB.</p>
  <hr class="my-4">
  <p>Aqui você pode criar e ler postagens!</p>
  <a class="btn btn-primary btn-lg" href="/usuarios/registro" role="button">Crie uma conta</a>
</div>

<hr>

<h2>Postagens recentes:</h2>

{{#each postagens}}
<div class="card mt-3">
    <div class="card-body">
        <small>Categoria: {{categoria.nome}}</small>
        <hr>
        <h3>{{titulo}}</h3>
        <p style="text-align: justify">{{descricao}}</p>
        <a href="/postagem/{{slug}}"><button class="btn btn-success">Leia Mais</button></a>
        <hr>
        <small>Data de publicação: {{data}}</small>
    </div>
</div>
{{else}}
    <h5>Nenhuma postagem!</h5>
{{/each}}